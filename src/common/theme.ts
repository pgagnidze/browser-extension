import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    secondary: '#0f172a',
    cardBg: '#1e293b',
    info: '#3b82f6',
    text: '#d8dee9',
  },
});

export default theme;
